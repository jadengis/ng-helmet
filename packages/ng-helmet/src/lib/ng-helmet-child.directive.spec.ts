import { NgHelmetChildDirective } from "./ng-helmet-child.directive";

describe("NgHelmetChildDirective", () => {
  it("should create an instance", () => {
    const directive = new NgHelmetChildDirective();
    expect(directive).toBeTruthy();
  });
});
